spring:
  #  스프링부트에서는 별도의 속성이 없으면 알아서 메모리 환경에서 테스트를 진행하게 된다.
  #  datasource:
  #    url: jdbc:h2:mem:test
  #    username: sa
  #    password:
  #    driver-class-name: org.h2.Driver
  #
  #  # JPA Setting
  #  jpa:
  #    hibernate:
  #      ddl-auto: create
  #    properties:
  #      hibernate:
  #        #show_sql: true
  #        format_sql: true

  # Console Font Color
  output:
    ansi:
      enabled: always

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

